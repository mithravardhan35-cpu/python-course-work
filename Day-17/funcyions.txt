Function:
            function is a  block of reusable code used to perform specific task
Syntax:
    def function_name():
    # code
Example:
    def greet():
    print("hello")
    greet()
    
Advantages:

1. code reusability – the same function can be used multiple times.
   example: in an online shopping app, the `calculate_total()` function can be used for every customer’s order.

2. reduces code repetition – avoids writing the same code again and again.
   example: in a student management system, one `calculate_percentage()` function can calculate percentages for all students.

3. improves readability – makes the program easy to read and understand.
   example: in a banking app, using functions like `deposit()`, `withdraw()`, and `check_balance()` makes the code easy to understand.

4. easy debugging – helps find and fix errors easily.
   example: if the login feature is not working, we can check and fix only the `login()` function.

5. easy maintenance – changes can be made in one function easily.
   example: if delivery charges change in a food delivery app, we only need to update the `calculate_delivery_charge()` function.

6. modular programming – divides a large program into smaller parts.
   example: in an ATM system, separate functions can be used for `deposit()`, `withdraw()`, and `check_balance()`.

7. saves time – reduces the time needed to write repeated code.
   example: a `send_email()` function can quickly send confirmation emails to many users.

8. well organized code – keeps the program structured and clean.
   example: in an e-commerce app, separate functions can manage products, payments, orders, and users.

9. reduces complexity – makes large programs easier to manage.
   example: in a hospital management system, separate functions can handle patient registration, appointments, and billing.

10. improves productivity – helps developers write programs faster and efficiently.
    example: developers can reuse an existing `validate_email()` function instead of creating email validation code every time.


Examples:

1. calculator app – a function can be used to add, subtract, multiply, or divide numbers.

2. login system – a function can check whether the username and password are correct.

3. online shopping – a function can calculate the total price of products.

4. atm system – a function can check the balance, deposit money, or withdraw money.

5. student result system – a function can calculate the total marks and percentage.

6. social media app – a function can handle login, posting, liking, and commenting.

7. food delivery app – a function can calculate the total bill and delivery charges.

8. banking application – a function can transfer money from one account to another.

9. attendance system – a function can mark a student or employee as present or absent.

10. interviewiq project – a function can evaluate an answer and generate feedback for the user.